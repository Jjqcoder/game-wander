name: CD

on:
    push:
        branches: [master]

jobs:
    deploy:
        runs-on: ubuntu-latest
        steps:
            - name: SSH & deploy
              uses: appleboy/ssh-action@master
              with:
                  host: ${{ secrets.HOST }}
                  port: 22
                  username: ${{ secrets.USER }}
                  key: ${{ secrets.SSH_KEY }}
                  fingerprint: ${{ secrets.HOST_FINGERPRINT }}
                  use_insecure_cipher: true # 关键参数
                  timeout: 30s
                  script: |
                      set -x
                      cd /root/game-wander
                      git pull origin main
                      docker-compose up -d --build
